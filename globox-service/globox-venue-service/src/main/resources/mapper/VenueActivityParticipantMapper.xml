<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.unlimited.sports.globox.venue.mapper.VenueActivityParticipantMapper">

    <!-- 批量查询用户的参与活动次数 -->
    <select id="selectUserParticipationCount" resultType="java.util.Map">
        SELECT user_id as userId, COUNT(*) as count
        FROM venue_activity_participant
        WHERE user_id IN
        <foreach item="userId" index="index" collection="userIds" open="(" separator="," close=")">
            #{userId}
        </foreach>
        GROUP BY user_id
    </select>

</mapper>
